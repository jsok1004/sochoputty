<Window x:Class="SochoPutty.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:SochoPutty.Converters"
        Title="Socho Putty Manager" Height="750" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackground}"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="CanResize">

    <Window.Resources>
        <converters:BooleanToStatusConverter x:Key="BooleanToStatusConverter"/>
        <converters:BooleanToBrushConverter x:Key="BooleanToBrushConverter"/>
    </Window.Resources>
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                     ResizeBorderThickness="5"
                     UseAeroCaptionButtons="False"
                     NonClientFrameEdges="None"
                     GlassFrameThickness="0"
                     CornerRadius="0" />
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/> <!-- 커스텀 타이틀바 -->
            <RowDefinition Height="Auto"/> <!-- 메뉴바 -->
            <RowDefinition Height="Auto"/> <!-- 툴바 -->
            <RowDefinition Height="*"/> <!-- 메인 컨텐츠 -->
        </Grid.RowDefinitions>
        
        <!-- 커스텀 타이틀바 -->
        <Grid Grid.Row="0" Background="{DynamicResource TitleBarBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- 아이콘과 타이틀 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                <Image Source="app.ico" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Socho Putty Manager" 
                          FontSize="14" 
                          FontWeight="Normal"
                          Foreground="{DynamicResource TitleBarForeground}" 
                          VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- 윈도우 컨트롤 버튼 -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Name="btnMinimize" 
                        Width="46" Height="32" 
                        Background="Transparent" 
                        BorderThickness="0"
                        Foreground="{DynamicResource TitleBarForeground}"
                        Click="MinimizeWindow_Click"
                        ToolTip="최소화">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource TitleBarButtonHover}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource TitleBarButtonPressed}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="🗕" FontSize="12"/>
                </Button>
                
                <Button Name="btnMaximize" 
                        Width="46" Height="32" 
                        Background="Transparent" 
                        BorderThickness="0"
                        Foreground="{DynamicResource TitleBarForeground}"
                        Click="MaximizeWindow_Click"
                        ToolTip="최대화">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource TitleBarButtonHover}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource TitleBarButtonPressed}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <TextBlock Name="btnMaximizeIcon" Text="🗖" FontSize="12"/>
                </Button>
                
                <Button Name="btnRestore" 
                        Width="46" Height="32" 
                        Background="Transparent" 
                        BorderThickness="0"
                        Foreground="{DynamicResource TitleBarForeground}"
                        Click="RestoreWindow_Click"
                        ToolTip="이전 크기로 복원"
                        Visibility="Collapsed">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource TitleBarButtonHover}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource TitleBarButtonPressed}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="🗗" FontSize="12"/>
                </Button>
                
                <Button Name="btnClose" 
                        Width="46" Height="32" 
                        Background="Transparent" 
                        BorderThickness="0"
                        Foreground="{DynamicResource TitleBarForeground}"
                        Click="CloseWindow_Click"
                        ToolTip="닫기">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E81123"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#F1707A"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="🗙" FontSize="12"/>
                </Button>
            </StackPanel>
        </Grid>
       
        <!-- 툴바 -->
        <Menu Grid.Row="1" Background="{DynamicResource MenuBackground}">
            <MenuItem Header="도구(_T)">
                <MenuItem Header="설정(_S)" Click="Settings_Click"/>
                <Separator/>
                <MenuItem Header="디버그(_D)">
                    <MenuItem Header="디버그 로그 보기" Click="ShowDebugLog_Click"/>
                    <MenuItem Header="로그 폴더 열기" Click="OpenLogFolder_Click"/>
                    <MenuItem Header="로그 초기화" Click="ClearDebugLog_Click"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="정보(_A)" Click="About_Click"/>
            </MenuItem>
            <MenuItem Header="새 연결(_N)" Click="NewConnection_Click"/>
            <MenuItem Header="연결 관리(_M)" Click="ManageConnections_Click"/>
            <ComboBox Name="cmbQuickConnect" Width="180" 
                                  SelectionChanged="QuickConnect_SelectionChanged"
                                  IsEditable="True"
                                  Text="빠른 연결 선택..."/>
            <MenuItem Header="🔗" Click="QuickConnect_Click"/>
            <MenuItem Header="⬚분할 해제(_Q)" Click="UnsplitAll_Click"/>
            <MenuItem Header="↕가로 분할(_A)" Click="SplitHorizontal_Click"/>
            <MenuItem Header="↔새로 분할(_S)" Click="SplitVertical_Click"/>
            <MenuItem Header="🔠4화면 분할(_D)" Click="SplitQuad_Click"/>
        </Menu>
        
        <!-- 메인 컨텐츠 영역 -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Name="splitContainer">
                <!-- 분할 영역들이 동적으로 추가될 Grid -->
                <!-- 기존 TabControl 제거 - SplitManager가 모든 것을 관리 -->
            </Grid>

            <Border Grid.Column="1"
                    Background="{DynamicResource MenuBackground}"
                    BorderBrush="{DynamicResource TitleBarBackground}"
                    BorderThickness="1,0,0,0"
                    Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="3*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="P2P 채팅"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TitleBarForeground}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0" VerticalAlignment="Center">
                            <TextBlock Text="내 IP:"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TitleBarForeground}"
                                       Margin="0,0,6,0"/>
                            <TextBlock Name="txtLocalIpAddress"
                                       Text="확인 중..."
                                       Foreground="{DynamicResource TitleBarForeground}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0" HorizontalAlignment="Left">
                            <Button Name="btnRefreshPeers"
                                    Content="사용자 검색"
                                    Click="RefreshPeers_Click"
                                    Padding="8,4"
                                    Margin="0,0,6,0"/>
                            <TextBlock Name="txtChatStatus"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TitleBarForeground}"
                                       Text="대기 중"/>
                        </StackPanel>
                    </StackPanel>

                    <GroupBox Header="사용자"
                              Grid.Row="1"
                              Margin="0,0,0,12">
                        <ListBox Name="lstPeers"
                                 SelectionChanged="Peers_SelectionChanged"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical" Margin="4,4,4,4">
                                        <TextBlock Text="{Binding IpAddress}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding IsOnline, Converter={StaticResource BooleanToStatusConverter}}"
                                                   FontStyle="Italic"
                                                   Foreground="{Binding IsOnline, Converter={StaticResource BooleanToBrushConverter}}"
                                                   FontSize="11"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </GroupBox>

                    <GroupBox Header="대화"
                              Grid.Row="2"
                              Margin="0,0,0,12">
                        <ListBox Name="lstMessages"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="4">
                                        <TextBlock FontSize="12" FontWeight="Bold"
                                                   Text="{Binding SenderIpAddress}"/>
                                        <TextBlock FontSize="12"
                                                   Text="{Binding Content}"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock FontSize="10"
                                                   Foreground="Gray"
                                                   Text="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </GroupBox>

                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Name="txtChatInput"
                                 Grid.Column="0"
                                 Height="32"
                                 Margin="0,0,6,0"
                                 KeyDown="ChatInput_KeyDown"/>
                        <Button Name="btnSendChat"
                                Grid.Column="1"
                                Content="전송"
                                Width="60"
                                Click="SendChat_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window> 