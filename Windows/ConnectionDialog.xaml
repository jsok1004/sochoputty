<Window x:Class="SochoPutty.Windows.ConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="연결 설정" Height="580" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource TextForeground}"
        WindowStyle="None"
        AllowsTransparency="False">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                     ResizeBorderThickness="5"
                     UseAeroCaptionButtons="False"
                     NonClientFrameEdges="None"
                     GlassFrameThickness="0"
                     CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/> <!-- 커스텀 타이틀바 -->
            <RowDefinition Height="*"/> <!-- 메인 컨텐츠 -->
            <RowDefinition Height="Auto"/> <!-- 버튼 영역 -->
        </Grid.RowDefinitions>
        
        <!-- 커스텀 타이틀바 -->
        <Grid Grid.Row="0" Background="{DynamicResource TitleBarBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- 아이콘과 타이틀 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                <Image Source="../app.ico" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="연결 설정" 
                          FontSize="14" 
                          FontWeight="Normal"
                          Foreground="{DynamicResource TitleBarForeground}" 
                          VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- 윈도우 컨트롤 버튼 -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Name="btnClose" 
                        Width="46" Height="32" 
                        Background="Transparent" 
                        BorderThickness="0"
                        Foreground="{DynamicResource TitleBarForeground}"
                        Click="CloseWindow_Click"
                        ToolTip="닫기">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E81123"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#F1707A"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="🗙" FontSize="12"/>
                </Button>
            </StackPanel>
        </Grid>
        
        <!-- 메인 컨텐츠 -->
        <Grid Grid.Row="1" Margin="10">
        
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 기본 정보 -->
                <GroupBox Header="기본 정보" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Label Grid.Row="0"  Grid.Column="0" Content="연결 이름:" Foreground="{DynamicResource TextForeground}"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Name="txtName" Margin="5"/>
                        
                        <Label Grid.Row="1" Grid.Column="0" Content="호스트명:" Foreground="{DynamicResource TextForeground}"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Name="txtHostname" Margin="5"/>
                        
                        <Label Grid.Row="2" Grid.Column="0" Content="포트:" Foreground="{DynamicResource TextForeground}"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Name="txtPort" Margin="5"/>
                        
                        <Label Grid.Row="3" Grid.Column="0" Content="프로토콜:" Foreground="{DynamicResource TextForeground}"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Name="txtProtocol" Margin="7,0,0,0"
                                   Text="SSH (OpenSSH)" VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextForeground}"/>

                        <Label Grid.Row="4" Grid.Column="0" Content="설명:" Foreground="{DynamicResource TextForeground}"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Name="txtDescription" Margin="5" 
                                 TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>
                    </Grid>
                </GroupBox>
                
                <!-- 인증 정보 -->
                <GroupBox Header="인증 정보" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Label Grid.Row="0" Grid.Column="0" Content="사용자명:" Foreground="{DynamicResource TextForeground}"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Name="txtUsername" Margin="5"/>

                        <Label Grid.Row="1" Grid.Column="0" Content="비밀번호:" Foreground="{DynamicResource TextForeground}"/>
                        <PasswordBox Grid.Row="1" Grid.Column="1" Name="pwdPassword" Margin="5"/>

                        <Label Grid.Row="2" Grid.Column="0" Content="개인키 파일:" Foreground="{DynamicResource TextForeground}"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Name="txtPrivateKeyPath" Margin="5"/>
                        <Button Grid.Row="2" Grid.Column="2" Name="btnBrowseKey" Content="찾기..."
                                Margin="5" Width="60" Click="BrowseKey_Click"/>

                        <TextBlock Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Margin="5,0,5,0"
                                   TextWrapping="Wrap" FontSize="11" Foreground="Gray"
                                   Text="※ OpenSSH는 비밀번호를 자동 전송할 수 없습니다. 접속 후 터미널에 직접 입력하거나 개인키(OpenSSH 형식) 인증을 사용하세요."/>
                    </Grid>
                </GroupBox>
                
                <!-- 테스트 연결 -->
                <GroupBox Header="연결 테스트">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Button Grid.Row="0" Name="btnTestConnection" Content="연결 테스트" 
                                Width="100" HorizontalAlignment="Left" Margin="0,0,0,10"
                                Click="TestConnection_Click"/>
                        
                        <TextBlock Grid.Row="1" Name="txtTestResult" TextWrapping="Wrap" 
                                   Foreground="Green" FontWeight="Bold"/>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
        </Grid>
        
        <!-- 버튼 영역 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,10,10,10">
            <Button Name="btnOK" Content="확인" Width="80" Margin="0,0,10,0" Click="OK_Click" IsDefault="True"/>
            <Button Name="btnCancel" Content="취소" Width="80" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window> 